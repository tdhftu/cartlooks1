  <div class="theme-option-sticky d-flex align-items-center justify-content-between bg-white border-bottom2 p-3">
      <div class="theme-option-logo">
          <h4>{{ translate('Business Settings') }} @if (isset($sub_link))
                  <span class="text-primary font-weight-normal">-> {{ translate($sub_link) }}</span>
              @endif
          </h4>
      </div>
  </div>
